<p id="notice"><%= notice %></p>

<h1>Concert Log</h1>
<h3>details and actions on my concert logs</h3>

<hr><br>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Artists</th>
      <th>Venue</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @logs.each do |log| %>
      <tr>
        <td><%= log.name %></td>
        <td><%= log.artists %></td>
        <td><%= log.venue %></td>
        <td><%= link_to 'Show', log %></td>
        <td><%= link_to 'Edit', edit_log_path(log) %></td>
        <td><%= link_to 'Destroy', log, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Log', new_log_path %>
